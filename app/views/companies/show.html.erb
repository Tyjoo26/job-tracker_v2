All Jobs for <strong><%= @company.name %></strong>
<br>

<% @company.jobs.each do |job| %>
  <ul><%= job.title %></ul>
<% end %><br>
<% @company.contacts.each do |contact| %><br>
<%= contact.full_name %><br>
<%= contact.position %><br>
<%= contact.email %>
<% end %>
<%= render partial: "/contacts/form" %>
<br>
<%= link_to "Create a Job for #{@company.name}", new_company_job_path(@company)%>
